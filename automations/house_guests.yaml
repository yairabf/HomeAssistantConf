  alias: House Guests
  description: ''
  trigger:
  - entity_id: input_boolean.house_guests
    platform: state
  condition: []
  action:
    service_template: >
        {% if is_state('input_boolean.house_guests', 'on') -%} 
        switch.turn_on
        {%- else -%}
        switch.turn_off
        {%- endif %}
    data:
      entity_id: switch.house_guests
  mode: single