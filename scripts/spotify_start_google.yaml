spotify_start_google:
  alias: 'Spotify: Start Playback'
  sequence:
        - service: spotcast.start
          data:
            device_name: "{{ states('input_select.chromecast_audio') }}"
            force_playback: true
  mode: single
  icon: mdi:spotify